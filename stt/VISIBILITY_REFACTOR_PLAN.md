# STT Visibility — Completed

The visibility refactor is complete. The `stt` module now exposes a minimal
public API surface of a single type: **`SpeechToText`**.

See `stt/README.md` and `stt/build.gradle.kts` (`checkSttApiSurface`) for
the enforced public API contract.
